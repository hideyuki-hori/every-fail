{
  "name": "every-fail",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @every-fail/web dev",
    "ef": "node apps/cli/src/main.ts",
    "build": "pnpm -r build",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10"
  }
}
